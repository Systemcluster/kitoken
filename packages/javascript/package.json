{
    "name": "kitoken",
    "displayName": "Kitoken",
    "description": "Fast and versatile tokenizer for language models, supporting BPE and Unigram tokenization",
    "version": "0.0.0",
    "author": {
        "name": "Christian Sdunek",
        "email": "me@systemcluster.me"
    },
    "license": "BSD-2-Clause",
    "repository": {
        "type": "git",
        "url": "github:Systemcluster/kitoken"
    },
    "engines": {
        "node": ">=18"
    },
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        },
        "./node": {
            "types": "./dist/node.d.ts",
            "default": "./dist/node.js"
        },
        "./web": {
            "types": "./dist/web.d.ts",
            "default": "./dist/web.js"
        }
    },
    "files": [
        "./dist/*"
    ],
    "scripts": {
        "build": "wasm-pack build --release --no-pack --target bundler --out-dir dist --out-name index && wasm-pack build --no-pack --target nodejs --out-dir dist --out-name node && wasm-pack build --no-pack --target web --out-dir dist --out-name web",
        "prepare": "wasm-pack build --release --no-pack --target bundler --out-dir dist --out-name index && wasm-pack build --no-pack --target nodejs --out-dir dist --out-name node && wasm-pack build --no-pack --target web --out-dir dist --out-name web"
    },
    "dependencies": {},
    "devDependencies": {
        "wasm-pack": "^0.12.1"
    }
}
